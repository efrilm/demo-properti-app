import 'package:flutter/material.dart';

class SalesSoldPage extends StatelessWidget {
  const SalesSoldPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
